Original prompt: 참고 이미지처럼 곡선 레일의 랜턴 2개와 종이 오브젝트 3개로 새 실루엣을 완성하는 플레이 가능한 게임을 만들고, 추후 난이도에 따라 랜턴/오브젝트를 추가할 수 있게 설계. 필요한 이미지는 분리 생성.

## 진행 기록

- 참고 이미지 분석 완료. 9:16 종이극장 구성, 양쪽 색광, 곡선 레일, 성공 시 새가 깨어나는 연출을 핵심으로 선정.
- 생성형 이미지 에셋 2종 분리 생성: `public/assets/dark-hanji-bg.png`, `public/assets/warm-hanji-screen.png`.
- 게임 수학 모듈 초안: 레일 좌표, 랜턴 위치, 3개 오브젝트가 만드는 4개 그림자 조각, 폴리곤 기반 일치율(Jaccard) 계산.
- Canvas 기반 전체 게임 화면 구현: 시작 안내, 종이극장, 오브젝트 3개, 컬러 광원, 곡선 레일 드래그, 점수 HUD, 성공 연출, 재시작, 키보드/전체화면 지원.
- `STAGE_ONE` 데이터 설정으로 랜턴·오브젝트·임계값 분리. 향후 스테이지 확장 지점 문서화.
- 초기 시안 엔진에서는 성공 임계값을 84%로 조정했으나, 아래 실제 투영 엔진 교체 후 폐기·재조정함.

## TODO

- 현재 요청 범위의 미완료 항목 없음.

## 최종 검증

- 최초 시안 엔진 기준 `npm test`: 6/6 통과.
- `npm run build`: Vite 프로덕션 빌드 성공.
- `vite preview` + HTTP 확인: HTML, 짙은 한지 PNG, 밝은 한지 PNG 모두 200 응답.
- 사용자 지침에 따라 브라우저/Chrome/computer-use 기반 화면 자동 조작은 수행하지 않음.

## 실제 그림자 엔진 교체

- 이전 `getShadowFragments` 방식은 미리 만든 목표 조각을 랜턴 값으로 변형한 시안 수준 구현이었으며 폐기함.
- `x/y/z` 2.5D 공간, 점광원, 깊이를 가진 불투명 오브젝트, `z=0` 스크린으로 모델 교체.
- 모든 오브젝트 꼭짓점에 대해 `L + k(P-L)`와 스크린의 교점을 계산하는 실제 원근 투영 구현.
- 두 광원이 각각 세 오브젝트 전체를 투영하고, 모든 빛이 차단된 완전 그림자를 목표 마스크와 비교하도록 변경.
- 목표 마스크 역시 정답 랜턴 위치에서 동일한 투영 알고리즘으로 생성하며 별도 수동 실루엣 좌표 제거.
- 일반화된 `calculateShadowSimilarity(lanternPositions, stage)`로 추후 3개 이상 랜턴 지원.
- 실제 투영 결과의 점수 분포에 맞춰 성공 임계값 90% 적용(양쪽 약 27px 오차까지 성공, 약 41px 오차는 실패).
- 광학 불변식, 모든 광원/오브젝트 조합, 3번째 랜턴 확장을 포함한 단위 테스트 10개 통과, 프로덕션 빌드 성공.
